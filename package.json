{
  "name": "rm-cafe",
  "version": "1.0.0",
  "description": "RM Cafe web app UI project",
  "scripts": {
    "start": "live-server . --open=index.html",
    "server": "live-server . --open=index.html",
    "server:chrome": "live-server . --open=index.html --browser=chrome",
    "server:edge": "live-server . --open=index.html --browser=edge"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "swiper": "^12.2.0"
  }
}
